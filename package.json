{
	"name": "@cjkihl/monorepo",
	"version": "0.0.1",
	"private": true,
	"workspaces": {
		"packages": [
			"packages/*"
		]
	},
	"scripts": {
		"build": "turbo build",
		"changeset": "changeset",
		"resolve-deps": "bun run ./packages/changeset-dep-resolver/index.bin.ts",
		"version": "changeset version",
		"release": "bun run build && changeset publish",
		"clean": "git clean -fxd -e .env.local",
		"lint": "biome check .",
		"lint-fix": "biome check --write --unsafe .",
		"test": "bun test",
		"up": "bunx npm-check-updates -ws --root --format group -u"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.0.0",
		"@changesets/cli": "^2.29.4",
		"bun-types": "latest",
		"turbo": "^2.5.4",
		"typescript": "^5.8.3"
	},
	"packageManager": "bun@1.2.17"
}
